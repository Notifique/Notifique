<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

  <item
      android:id="@+id/search_notifiques"
      android:icon="@drawable/toolbar_search"
      android:title="@string/toolbar_search"
      app:showAsAction="ifRoom|collapseActionView"
      app:actionViewClass="android.widget.SearchView"/>

  <item
      android:id="@+id/deselect"
      android:icon="@drawable/toolbar_back"
      android:title="@string/toolbar_deselect"
      android:visible="false"
      app:showAsAction="ifRoom" />

  <item
      android:id="@+id/delete"
      android:icon="@drawable/toolbar_delete"
      android:title="@string/toolbar_delete"
      android:visible="false"
      app:showAsAction="ifRoom" />
</menu>
